# 项目本地 Claude 说明，只在本机（用户定）
CLAUDE.md

# 任务过程文档：需求 / 设计 / 任务拆分 / 状态 / 派发说明 / worker 报告
# 只在本机保留，不入仓
task/

# 项目级远程模式开关 state.json，只在本机
.lark-connector/

# Node 工具链（package.json / tsconfig / scripts 都在仓根）：
# 依赖、tsc 的测试编译产物、卡片预览的输出
node_modules/
.test-out/
card-preview.html

# 编辑器本地配置
.vscode/

.DS_Store
