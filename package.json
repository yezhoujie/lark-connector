{
  "name": "lark-connector",
  "description": "Build, type-check and test toolchain for lark-connector — nothing in this package ships",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "node scripts/test.mjs"
  },
  "dependencies": {
    "@larksuite/channel": "^0.7.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/qrcode": "^1.5.5",
    "typescript": "^5.7.0",
    "esbuild": "^0.25.0"
  },
  "license": "MIT",
  "private": true
}
