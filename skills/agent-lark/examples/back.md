---
description: 我回来了 —— 关闭飞书远程模式，回到终端交互
---
用户回到键盘前了。

1. 先看有没有还挂在手机上的提问（`herdr-lark status` 里「挂起提问」不为 0，或你自己有在跑的后台 ask 任务）。
   有的话先停掉那个后台任务，飞书卡片会变成「⚠️ 已取消」——不要让用户手机上留一张死卡。
2. 执行 `herdr-lark away off`，把 stdout 转告用户。

daemon 留着不用停。之后照常在终端交互。
