# plugin/lark-connector

Reserved for a future Claude Code plugin (a hooks module) built on top of the `lark-connector` daemon.
There is nothing here yet, and nothing here is published. Do not put a `SKILL.md` in this directory —
the skill lives in `skill/agent-lark/`.
